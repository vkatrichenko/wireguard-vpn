# Tasks: Dashboard Binary via GitHub Releases

- **Functional Spec:** [functional-spec.md](./functional-spec.md)
- **Technical Spec:** [technical-considerations.md](./technical-considerations.md)
- **Stack:** Go dashboard (`dashboard/`, ldflags), GitHub Actions (`.github/workflows/`), Terraform (`terraform/dev/` + `terraform/modules/{wireguard,dashboard,github-oidc}`), EC2 user-data (`terraform/modules/wireguard/templates/user-data.txt`).

Each slice leaves the tree in a **validate-able, runnable** state. In-session proof is `go test` / `go build` + run (Slice 1), workflow static-lint + reasoning (Slice 2), `terraform validate` + `plan` + a shell test of the checksum logic (Slices 3–4). The real boot/fetch and the tag-triggered release require an operator **apply** + a **public repo** + a **tag push** — those are the manual Slice 5, honestly out of in-session reach (no `terraform apply`, pushes are gated, repo-public is a separate precondition).

> **Sequencing & precondition.** Slices 3–5 only work once the repository is **public** (anonymous asset download) and a release **actually exists** for the pinned tag (Slice 2 must have produced one). Apply Slices 3 + 4 in a **single** `terraform apply` (the curl-from-release path replaces the `aws s3 cp` path in the same change that removes the S3/IAM it depended on). Slices 1–2 are pure app/CI and land independently of any apply.

---

- [x] **Slice 1: The binary self-reports its release tag**
  - [x] Add a `ReleaseTag = "dev"` build-time var alongside `BuildSHA`/`BuildTime`/`GoVersion` in `dashboard/cmd/wireguard-dashboard/main.go`, and thread it into the existing build-info struct so it surfaces on the **About** tab (and any `/api` snapshot that already carries build metadata). **[Agent: go-fullstack]** — added `ReleaseTag` to `main` vars + `serverinfo.BuildInfo` (new `release_tag` JSON field) + `aboutBinaryCardData`; About card now renders a "Release" row above "Build SHA".
  - [x] Extend the `dashboard/Makefile` `LDFLAGS` with `-X main.ReleaseTag=$(RELEASE_TAG)` (default `dev` when unset, mirroring the `git rev-parse --short` fallback pattern already there). **[Agent: go-fullstack]** — `RELEASE_TAG ?= dev`, injected via `-X main.ReleaseTag`.
  - [x] Update/extend the build-info unit test(s) and the About-tab render test to assert the release tag is present (and defaults to `dev` when not injected). **[Agent: go-fullstack]** — `TestHandleGetPartialAbout_RendersAllCards` now seeds `ReleaseTag: "v1.2.3"` and asserts the `Release` label + `v1.2.3` cell render.
  - [x] **Verify:** `go test ./...` in `dashboard/` green; `gofmt`/`go vet` clean; `make build RELEASE_TAG=v0.0.1 && ./wireguard-dashboard` (or a curl against the snapshot/About endpoint with injected fakes) shows `v0.0.1`; with no override it shows `dev`. **[Agent: go-fullstack]** — `go test ./...` all `ok`; `go vet` clean; touched files `gofmt`-clean; `make build RELEASE_TAG=v0.0.1` embeds `v0.0.1` in the binary (verified via `strings`); default build carries the `dev` sentinel.

- [x] **Slice 2: Tag-triggered GitHub Release (build + test + SHA256SUMS), no AWS**
  - [x] Replace `.github/workflows/dashboard-build.yml` with a **release** workflow: `on: push: tags: ['v*.*.*']`, `permissions: contents: write` only (no `id-token`, no AWS OIDC). Steps: checkout → setup-go (from `dashboard/go.mod`) → `go vet` → `go test -race ./...` → cross-compile `linux/amd64` with existing ldflags **plus** `-X main.ReleaseTag=${GITHUB_REF_NAME}` → generate `SHA256SUMS` over the binary → create the GitHub Release for the tag and upload `wireguard-dashboard` + `SHA256SUMS`. **[Agent: cicd-github-actions]** — created `.github/workflows/dashboard-release.yml` (publishes via `gh release create` + `${{ github.token }}`, no third-party action to SHA-pin; reused the repo's pinned `checkout`/`setup-go` SHAs); `dashboard-build.yml` removed (`gh release` model supersedes the S3 upload). Kept `go vet` + `go test -race` to preserve the prior quality bar.
  - [x] Delete `.github/workflows/dashboard-deploy.yml` (the SSM hot-swap path is decommissioned; removal of its IAM/SSM backing is Slice 4). **[Agent: cicd-github-actions]** — `git rm`'d; only `dashboard-release.yml` remains.
  - [x] Remove every AWS touchpoint from the release job: no `configure-aws-credentials`, no `aws s3 cp`, no `role-to-assume`. Confirm the job references **no** AWS ARN, region, or bucket. **[Agent: cicd-github-actions]** — grep of non-comment lines confirms zero `aws`/`s3`/`oidc`/`ssm`/account-id references in executable content.
  - [x] **Verify:** static-lint the workflow (`actionlint` if available, else a careful YAML + trigger/permission review); confirm `v*.*.*`-only trigger, `contents: write`, the `ReleaseTag` ldflag wired to `GITHUB_REF_NAME`, and `SHA256SUMS` covers the uploaded binary. **Real proof (operator):** push a throwaway pre-release tag (e.g. `v0.0.1-rc1`) on the public repo and confirm the binary + `SHA256SUMS` attach and the binary self-reports the tag — flagged in the table below (tag pushes are gated; no in-session push). **[Agent: cicd-github-actions]** — `actionlint` not installed; verified via Ruby YAML parse (well-formed) + manual review: tags-only trigger, `contents: write`, `ReleaseTag=${GITHUB_REF_NAME}`, `sha256sum wireguard-dashboard > SHA256SUMS` (bare filename so it checks post-`curl -o`). Operator tag-push proof still pending.

- [x] **Slice 3: User-data fetches the pinned release over HTTPS with SHA256 verification**
  - [x] Add a pinned `dashboard_release_tag = "v0.0.1"` to `terraform/dev/locals.tf` (sitting with the pinned AMI as the single source of truth for the running version), plus a `dashboard_release_repo` slug (default `vkatrichenko/wireguard-vpn`) or full release-URL base. **[Agent: terraform-aws]** — added both locals with a comment tying the tag to the instance-replace-on-bump model.
  - [x] Thread `dashboard_release_tag` (+ repo/URL base) from `terraform/dev/main.tf` into the `wireguard` module; add the matching variables to `terraform/modules/wireguard/variables.tf` (with `description → type → default → validation` ordering and a SemVer-ish `validation` on the tag). Add them to the `local.user_data` `templatefile(...)` inputs in `terraform/modules/wireguard/locals.tf`. **[Agent: terraform-aws]** — `dashboard_release_tag` (default `""`, SemVer `validation` allowing pre-release suffixes) + `dashboard_release_repo` (default `vkatrichenko/wireguard-vpn`, `owner/name` `validation`); threaded via `main.tf`; templatefile map swaps `dashboard_artifact_bucket_name` → the two release vars.
  - [x] In `terraform/modules/wireguard/templates/user-data.txt`, replace the `aws s3 cp s3://${dashboard_artifact_bucket_name}/latest/wireguard-dashboard ...` line (~114) with: `curl -fsSL <release-base>/<tag>/wireguard-dashboard -o <tmp>` **and** `curl -fsSL <release-base>/<tag>/SHA256SUMS -o <tmp>.sums`, then `sha256sum -c` (filtered to the binary) **before** `chmod +x` + atomic `mv` into place. A `curl -f` HTTP error or a failed checksum must exit non-zero so the `.ready` health signal is never written. Keep the IMDSv2 token block and the S3 health-check `.ready` signaling untouched; the `%{ if dashboard_artifact_bucket_name != "" ~}` gate becomes `%{ if dashboard_release_tag != "" ~}`. **[Agent: linux-cloud-init]** — gate flipped; step 5 downloads binary+`SHA256SUMS` into a `mktemp -d`, each `curl`/`sha256sum -c --ignore-missing` wrapped in `if ! …; then echo FATAL >&2; exit 1; fi` (no global `set -e`, since the WG health loop relies on non-fatal non-zero exits); step 6 `install -m 0755` atomically. IMDSv2 + `.ready` untouched.
  - [x] Decide whether `awscli` is still needed in user-data — it is **retained** (the `.ready` health signal still uses `aws s3 cp` to the health-check bucket), so keep the `awscli` install and IMDSv2 blocks. Note this explicitly so it isn't mistaken for dead code. **[Agent: linux-cloud-init]** — retained; the dashboard section's header comment now states `awscli` is kept solely for the `.ready` signal.
  - [x] **Verify:** `terraform validate` in `terraform/dev/`; `terraform plan -out=tfplan` and read the diff — a tag value change must show **exactly one** `aws_instance` replacement + a new `aws_launch_template` version and nothing else stateful (confirm LT-version replacement semantics against provider `= 6.41.0` via the Terraform MCP if uncertain). Shell-test the checksum gate in isolation: a tampered/truncated binary fails `sha256sum -c` with a non-zero exit. Share the plan summary. **[Agent: terraform-aws]** — `terraform validate` → **"Success! The configuration is valid"** (post-SSO-refresh); `terraform fmt` clean; templatefile token↔map coverage balanced; SHA256 gate shell-tested (untampered→exit 0, tampered & truncated→exit 1). `terraform plan` is owner-run per repo policy — expected diff = one `aws_instance` replacement + new launch-template version, nothing else stateful (Slice 5 reads it on the real apply).

- [x] **Slice 4: Decommission the private S3 / SSM / deploy-IAM path**
  - [x] Cross-resource dependency scan first (per CLAUDE.md `/aws-teardown` ritual): grep the repo + sibling repos for the artifact bucket name, the deploy SSM document name, and the two CI role names; confirm nothing else references them. Report hits **before** writing any removal. **[Agent: devsecops-quality]** — scanned this repo + `~/Downloads/git-repos` + `~/Documents/internal-projects`: no external references; in-repo hits were only the files being edited plus non-blocking history/docs (002 spec, two `.claude/agents/*.md`, `geoip/README.md`).
  - [x] Remove the entire `terraform/modules/dashboard/` module (it exists solely for `s3.tf` artifacts + `deploy.tf` SSM/IAM) and its `module "dashboard"` block + the `dashboard_artifact_bucket_*` wiring in `terraform/dev/main.tf`. **[Agent: terraform-aws]** — `git rm -r modules/dashboard/`; removed the `module "dashboard"` + `module "github_oidc"` blocks (no AWS-auth CI remains), left an explanatory comment.
  - [x] In `terraform/modules/wireguard/`: drop `var.dashboard_artifact_bucket_arn` / `var.dashboard_artifact_bucket_name`, the two `dynamic "statement"` blocks granting `s3:GetObject` / `s3:ListBucket` on the artifacts bucket in `iam.tf`, and the `dashboard_artifact_bucket_name` input in `locals.tf`. **Keep** the health-check bucket grant and `AmazonSSMManagedInstanceCore` only if SSM is still wanted for break-glass — otherwise the spec removes the deploy path, so drop the deploy-specific pieces and re-confirm the `.ready` `s3:PutObject` grant stays. **[Agent: terraform-aws]** — both vars + both dynamic statements removed (locals input was already gone in Slice 3); `.ready` `s3:PutObject` grant retained; `AmazonSSMManagedInstanceCore` **kept for break-glass `aws ssm start-session`** (comment updated — its sole prior reference to the deploy path was stale).
  - [x] In `terraform/dev/main.tf` `module "github_oidc"`: remove the `dashboard-ci-build` role (its `s3_put_object`) and the `dashboard-ci-deploy` role (its `inline_policy_json`); a no-AWS release needs neither. Remove the now-dangling `dashboard_ci_build_role_arn` / `dashboard_ci_deploy_role_arn` / `dashboard_deploy_ssm_document_name` outputs in `terraform/dev/outputs.tf`. **[Agent: terraform-aws]** — whole `module "github_oidc"` block removed (both roles); `dev/outputs.tf` `git rm`'d (all 3 outputs were dashboard-CI-only). The `modules/github-oidc/` dir is left in place (unreferenced, reusable).
  - [x] **Verify:** `terraform validate` in `terraform/dev/`; `terraform plan` shows the artifacts bucket + its versioning/lifecycle/SSE/PAB, the SSM document, the deploy + build IAM roles/policies as **destroys**, with the **EIP, S3 state bucket, and health-check bucket untouched**. `make pre-commit` at repo root (fmt + docs + tflint + trivy) green. Share the destroy summary. **[Agent: devsecops-quality]** — owner ran `terraform plan`: **3 add / 2 change / 13 destroy** — dashboard S3 (×5) + SSM doc + both OIDC roles+policies destroyed; `wireguard_policy` loses the 2 dashboard S3 statements (in-place); LT user_data updated → one `aws_instance` replacement (+ `eip_association`/`status_check` re-create). **EIP address `3.216.7.94`, state bucket, and health-check bucket untouched.** (No dangling refs; `terraform fmt` clean. PENDING: `make pre-commit` to regenerate the now-stale `wireguard`/`dev` module READMEs + run tflint/trivy before commit.)

- [ ] **Slice 5: End-to-end on a real apply (operator-only)**
  - [ ] **Precondition:** repository is public **and** the pinned `dashboard_release_tag` has a real published release (Slice 2). **[Manual]**
  - [ ] Apply Slices 3 + 4 in a **single** `terraform apply` (per the repo workflow: `plan -out=tfplan` → owner review → `apply tfplan`). Confirm the plan shows one instance replacement + new LT version + the Slice-4 destroys and nothing else stateful. **[Manual]**
  - [ ] Observe `/var/log/cloud-init-output.log` on the new instance: the boot `curl` pulled the pinned tag, `sha256sum -c` passed, `wireguard-dashboard.service` is active, and the **About** tab shows the expected `vX.Y.Z`. EIP re-associated; clients reconnect with no config change. A green plan does **not** prove the fetch — the boot log must be read. **[Manual]**
  - [ ] Rollback rehearsal: re-pin an earlier tag → plan → apply → About shows the earlier version. **[Manual]**

---

## Items needing attention

| Task/Slice | Issue | Recommendation |
|---|---|---|
| Slice 2 real proof | Tag pushes are gated; no in-session `git push` / tag push | Owner pushes a throwaway `v0.0.1-rc1` on the **public** repo to confirm the release attaches the binary + `SHA256SUMS` and the binary self-reports the tag |
| Slice 2 lint | `actionlint` may not be installed locally | Install `actionlint` for real workflow linting, else rely on YAML + trigger/permission review |
| Slices 3–5 | Repo must be **public** for anonymous asset download; until then user-data `curl` 404s | Make the repo public (tracked precondition) before applying Slice 3 |
| Slice 4 | Removals are AWS deletions of shared infra | Run the cross-resource dependency scan first; owner runs the destroy via `apply` — never `-target`-destroy |
| Slice 5 | Needs `terraform apply` + a real EC2 boot + a tag push; not reproducible in-session | Owner runs it; treat as the required manual end-to-end per CLAUDE.md ("a green plan does not prove the binary fetch works") |
