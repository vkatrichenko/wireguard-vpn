# Tasks: Connection History & Geo Map

- **Functional Spec:** [functional-spec.md](./functional-spec.md)
- **Technical Spec:** [technical-considerations.md](./technical-considerations.md)
- **Stack:** Go std-lib HTTP + `html/template`/htmx + Chart.js (dashboard `dashboard/`). Pure-app change — no Terraform/IAM/network. All sub-tasks → `go-fullstack`.

**Confirmed decisions** (resolved `[NEEDS CLARIFICATION]`): session-gap threshold **10 min**; co-located map peers → **count badge** (no clustering lib); geo map → **card on the Clients tab**.

**Grounding (existing schema/seams):** handshake history derives from the existing `handshake_events(ts, public_key, name)` table (indexed on `ts`, already retention-swept — **no new table**, satisfies §2.2). `internal/geoip.Lookup(ip) → (country, city)` is extended to also return lat/lon (the GeoLite2-City record already carries `Location.Latitude/Longitude`). Reuse the 003 expand panel, time-range selector, 10s htmx tick, Chart.js, dark-mode theming, and `embed.FS` asset vendoring. Hard constraint: **zero outbound requests** (offline map).

Each slice leaves the dashboard runnable; in-session proof is `go test` + httptest + curl against injected fakes. Map rendering + offline guarantee need a manual visual check (no browser MCP), flagged in the table.

---

- [x] **Slice 1: Per-client online / last-seen / connection summary (data + expand-panel text)**
  - [x] Add a pure session-derivation package `internal/history/` (or funcs in `internal/db/`): given a peer's `handshake_events` ordered by `ts`, infer sessions (gap > **10 min** opens a new session), compute connected-time and session-count for a range, `online` = latest handshake within **3 min** (matching 003), and a human "last seen" string ("2 days ago" / "never"). No I/O — operates on passed-in samples. **[Agent: go-fullstack]**
  - [x] Unit-test the derivation: synthetic handshake sequences (continuous, gappy, single, none) asserting session count, connected-time, online/offline, last-seen rendering, and the **10-min gap boundary** (exactly-10 vs just-over). **[Agent: go-fullstack]**
  - [x] Add a `db` query returning a peer's `handshake_events` within a range (reuse the `idx_handshake_events_ts` index; filter by `public_key` + `ts BETWEEN`). **[Agent: go-fullstack]**
  - [x] Add `GET /api/clients/{name}/history?range={1h|6h|24h|7d}` (default 24h) → JSON: ordered session spans, total connected time, session count, online, lastSeen. `404` unknown client; empty history returns an empty timeline, not an error. Handler tests (`httptest` + fake store): each range, unknown → 404, empty history. **[Agent: go-fullstack]**
  - [x] Render the summary (online/last-seen + connected-time + session-count) in the **Clients expand panel** (extend the 003 detail panel), honoring the existing range selector. **[Agent: go-fullstack]**
  - [x] **Verify:** `go test ./...` in `dashboard/` green; `gofmt`/`go vet` clean; httptest covers the API (the curl-equivalent path); expand-panel render test asserts the summary fields. Confirm no new table/migration was added (derivation is query-time over `handshake_events`). **[Agent: go-fullstack]**

  > **Implementation notes (Slice 1):** New pure pkg `internal/history` (`Derive(handshakes, now, gapThreshold, onlineThreshold) Summary`; `SessionGapThreshold=10m` const, exclusive — exactly-10m stays one session). New `db.QueryHandshakeEventsByKey(ctx, publicKey, from, to)`. New route `GET /api/clients/{name}/history` — **keyed by manifest name** (resolved via `clientsfile.ByName`, matching the sibling `/config` route), then queried by the resolved public key; `onlineThreshold` (3m) injected from the server's existing const, not duplicated. Expand panel (`/partial/clients/{pubkey}/detail` + `client-detail.html`) extended with a status / session-count / connected-time summary. No new table — derivation is query-time over `handshake_events`. `go test ./...` green, `go vet` clean, my files gofmt-clean, static linux/amd64 build verified.

- [x] **Slice 2: Connection timeline visualization (online/offline bands) in the expand panel**
  - [x] Render the session spans from Slice 1 as a **timeline** in the expand panel over the selected range — online/offline bands (reuse Chart.js, e.g. a horizontal floating-bar, to avoid a new dependency); colors honor the dark-mode theme (003 §2.9). Fresh-host / insufficient-history shows whatever exists with no error. **[Agent: go-fullstack]**
  - [x] Wire the timeline into the existing 10s htmx refresh and the 1h/6h/24h/7d range selector; a failed refresh keeps last values + the global "Stale data" indicator (003 §2.11). **[Agent: go-fullstack]**
  - [x] **Verify:** render/partial test asserts the timeline canvas + data-range attribute present and bands reflect injected sessions; `go test` green. Visual legibility (incl. mobile) is the manual check below. **[Agent: go-fullstack]**

  > **Implementation notes (Slice 2):** New `web/static/client-timeline.js` IIFE (charts.js style) draws a Chart.js floating-bar (`type:"bar"`, `indexAxis:"y"`, each session `x:[start,end]` on a time scale bounded to `[from,to]`). **Server-embedded data, not fetched:** `clientDetailData` gained `Sessions`/`From`/`To`/`TimelineJSON template.JS`; the panel emits a `<script type="application/json">` block the JS reads — reuses Slice-1 derivation, no second round-trip, sidesteps the panel(pubkey)-vs-history-API(name) mismatch, and lets the render test assert bands-reflect-sessions server-side. Dark-mode via `--gridline`/`--text-muted`/`--accent` + `__themeChanged` (`chart.update("none")`); refresh/range ride the existing per-client-chart swap lifecycle (no new `hx-trigger` — none existed; matches throughput chart); stale-indicator untouched (shared `#stale-pill`). Single-handshake (zero-duration) bands widened by a 30s visual epsilon. Verified independently: `gofmt`/`go vet` clean, `go test ./...` green (2 new timeline render tests pass), zero-outbound grep clean, `node --check` on the JS OK, static linux/amd64 build OK. Exact band rendering + mobile legibility remain the manual Slice 5 check.

- [x] **Slice 3: GeoLite2 lat/lon extension + `/api/geo` (map data, no UI yet)**
  - [x] Extend `internal/geoip` to also surface `Location.Latitude/Longitude` (e.g. a `LookupGeo(ip) → {country, city, lat, lon, ok}`), treating missing coordinates like an unresolvable lookup. Keep the existing `Lookup` signature working for 003 callers. **[Agent: go-fullstack]**
  - [x] Add `GET /api/geo` → JSON: mappable peers `{name, lat, lon, city, country, online, lastSeen}` (reuse Slice-1 online/last-seen) for peers with a resolvable public endpoint + coords; **exclude** RFC1918 / unresolvable / missing-coord peers and return a `notMappable` count. **[Agent: go-fullstack]**
  - [x] Unit-test geoip lat/lon (known IP → expected coords; private/unresolvable → not-ok); handler test for `/api/geo` (mappable vs excluded, `notMappable` count) with injected geoip + clients/wg fakes. **[Agent: go-fullstack]**
  - [x] **Verify:** `go test ./...` green; `gofmt`/`go vet` clean; assert excluded peers never appear in the payload and the count is correct. **[Agent: go-fullstack]**

  > **Implementation notes (Slice 3):** `geoip.LookupGeo(ip) → GeoPoint{Country,City,Lat,Lon,OK}` (DB-IP `Location.Latitude/Longitude`); missing-coords gate = both lat&lon `== 0` → `OK=false` (Null-Island false-negative accepted). `Lookup(country,city)` unchanged — shares the guard set but does NOT route through `LookupGeo` (it still wants coord-less records' country/city for 003). `GeoResolver` interface extended with `LookupGeo` (one seam); `*geoip.Service` satisfies it, the two test fakes (`staticGeoResolver`, `fakeGeoResolver`) updated; `nil` geoipSvc still allowed (every peer → not-mappable, no panic). New `handlers_geo.go` → `GET /api/geo` → `{"peers":[{name,lat,lon,city,country,online,last_seen}],"not_mappable":N}` (**snake_case** per task prompt — Slice 4's JS must match these field names). `online`/`last_seen` from the peer's **live `LatestHandshake`** vs `onlineThreshold` + `humanAgo` (deliberate reuse of the live indicator, not an N-query-per-tick history scan). Excluded: pending peers, RFC1918/unresolvable, coord-less, nil-resolver; "unknown" wg-only peers not considered. Tests pin **no exact coords** (DB drifts monthly) — bounding-box + hemisphere only. Verified independently: `gofmt`/`go vet` clean, `go test ./...` green, static linux/amd64 build OK. **No UI** — map card is Slice 4.

- [x] **Slice 4: Offline world-map card on the Clients tab (embedded SVG + projected markers)**
  - [x] Vendor a permissively-licensed **SVG world outline** into `dashboard/web/static/` (embedded via `embed.FS`, served from `/static/`); record its source + license in-repo (repo is going open-source). No external tiles/fonts/scripts. **[Agent: go-fullstack]**
  - [x] Add a small static JS module (alongside the existing theme/chart scripts) doing the **equirectangular projection** `x=(lon+180)/360*W`, `y=(90-lat)/180*H` and absolutely positioning markers over the SVG; pull data from `/api/geo` on the 10s tick. Co-located peers → **one marker + count badge**; hover/tap shows name, city/country, online/last-seen. Online vs offline encoded by color/opacity with a **legend**; marker colors honor dark mode. **[Agent: go-fullstack]**
  - [x] Add the **map card to the Clients tab** template with the legend, a "**N not mappable**" caption, and the empty state ("No mappable peers."). Mobile per 003 §2.10 (scales to viewport width, no horizontal scroll ≥360px, touch targets ≥44px). **[Agent: go-fullstack]**
  - [x] Unit-test the projection function (known lat/lon → expected x/y within tolerance: corners, equator/prime-meridian, negative coords). **[Agent: go-fullstack]**
  - [x] **Verify (in-session):** `go test ./...` green; `gofmt`/`go vet` clean; partial/render test asserts the map card, legend, and not-mappable caption render; **grep templates + `web/static/` for any external URL** (`http(s)://`, CDN, font host) to prove zero outbound references. **[Agent: go-fullstack]**

  > **Implementation notes (Slice 4):** Base map `web/static/world.svg` (26 KB, 177 paths) derived from **Natural Earth 110m Admin-0** GeoJSON via mapshaper — **genuine Public Domain, no attribution** (confirmed from upstream LICENSE.md; NOT CC-BY/SA — clean for open-source). Forced full-globe **2:1 `viewBox="0 0 2000 1000"`** (synthetic frame polygon + `margin=0`, frame path stripped) so CRS84 maps linearly to lon/lat with no offset; source+sha256+license recorded in `VENDORED.txt`. New `web/static/world-map.js` IIFE: pure `project`/`projectPercent`/`clusterPeers` at top, DOM wiring behind `typeof document` guard, `module.exports` guard for node. Markers percent-positioned (track on resize); own 10s `/api/geo` tick; cluster by 1-decimal lat/lon → one marker + count badge, hover/tap lists co-located peers; online=`--success`/offline=`--text-muted`@0.55 + legend, re-themed on `__themeChanged`. Map card added to `clients.html` (renders unconditionally, even with 0 clients): embedded `<img src="/static/world.svg">` + `.geo-markers` overlay + legend + `#geo-not-mappable` caption + `.geo-empty` "No mappable peers." Dark-mode base via CSS `filter` on the `<img>` (keeps SVG cacheable, fragment small); 44px touch targets, `aspect-ratio:2/1`, no h-scroll ≥360px. **Tests:** node projection test `dashboard/jstest/world-map.test.mjs` (OUTSIDE embedded `web/`; import.meta-relative require) — 7/7 pass (corners, origin, negative, cities, percent, clustering, null-tolerance); run via `node --test` from `jstest/` (not in `make test` — no JS harness wired). Go `TestHandleGetPartialClients_RendersGeoMapCard` asserts card/legend/caption/empty-state + `world-map.js` reference. **Verified independently:** SVG sha256 matches record, only "external ref" is the `xmlns` namespace URI (not fetched), `world.svg` embedded in the binary, `gofmt`/`go vet` clean, `go test ./...` green, static linux/amd64 build OK, zero-outbound grep clean. Browser visual + true offline proof = manual Slice 5.

- [x] **Slice 5: End-to-end manual verification on the deployed dashboard (operator-assisted)**
  - [x] On a VPN client, load the Clients tab: confirm the **timeline** renders per client (online/offline bands, connected-time, session count) across ranges; the **map** renders **with no network** (offline — check devtools shows no outbound requests), markers land in plausible locations, online/offline encoding + legend read correctly, co-located peers show a count badge, the "N not mappable" caption is right, and both views are legible on a phone. Dark-mode colors apply. **[Manual]**

  > **Verification notes (Slice 5):** Operator-confirmed on the deployed dashboard (2026-06-25). **Map:** renders offline (no outbound requests), markers in plausible locations, online/offline encoding + legend correct, count badge + "N not mappable" caption correct, legible on mobile, dark-mode applies. **Timeline / history summary:** the per-client expand panel (click a client row) shows the throughput chart + timeline canvas + Status / Sessions (24h) / Connected (24h) — confirmed present and correct after expanding a row (the figures live in the expand panel, not the collapsed table row).

- [x] **Slice 6: Fix Clients-tab layout regression + map-card UX polish**
  > Regression introduced by Slice 4: the Clients tab now has two sibling cards (the client table `.card` + `#geo-map`), so `#tab-body`'s `repeat(auto-fit, minmax(300px,1fr))` grid (app.css:162) splits the row into two columns and **horizontally clips the 9-column client table** (Geo/Config columns cut off). Pure-app (CSS-led) fix — no API/geoip/template-logic change.
  - [x] Make the **client-table card span the full tab-body row** (`grid-column: 1 / -1`, matching the `#disk`/`#processes` pattern at app.css:477–483) so the table is never horizontally clipped at desktop widths. **[Agent: go-fullstack]**
  - [x] **Stack `#geo-map` below the table, size-constrained.** Full-width at ~1400px would render a 2:1 map ~700px tall; cap it (`max-width`/`max-height` on `.geo-map-frame`, centered) so "Peer locations" is reasonably sized, not dominating. Keep the 2:1 aspect so projection markers still align; preserve the dark-mode `<img>` filter. **[Agent: go-fullstack]**
  - [x] **Graceful narrow-viewport + legend styling.** The wide table degrades via `overflow-x:auto` **within the card** (no clipped page); the legend ("Online / Offline") renders as proper chips, not bare unstyled text (screenshot shows it unstyled). Mobile per 003 §2.10 (no horizontal *page* scroll ≥360px, touch targets ≥44px). **[Agent: go-fullstack]**
  - [x] **Verify:** `go test ./...` green; `gofmt`/`go vet` clean; existing `TestHandleGetPartialClients_RendersGeoMapCard` still passes; partial render asserts the client-table card + map card both carry the full-row span class. Manual: Clients tab shows all 9 columns un-clipped, map below at a sane size, both legible on mobile, dark-mode intact. **[Agent: go-fullstack]**

  > **Implementation notes (Slice 6):** Added `id="clients"` to the client-table `<section class="card">` (no `#clients` collision — the only grep hit was a `world-map.js` comment about the tab *hash*). Extended the existing full-row-span selector list with `#tab-body > #clients, #tab-body > #geo-map { grid-column: 1 / -1; }` so both cards stack (table on top, map below) instead of the auto-fit grid columning them. Table scroll handled card-level (no markup wrapper): `#clients { overflow-x: auto; }` + `.client-table { min-width: 720px; }` (same pattern as `#disk`/`#processes`) — wide table scrolls inside its card, never clips, never forces page scroll ≥360px; mobile `@media` needed no edit (a `1 / -1` span in a 1fr column is just the one column). Map capped: `.geo-map-frame { max-width: 760px; margin-inline: auto; }` (~380px tall) with **`aspect-ratio: 2/1` untouched** so the plate-carrée marker math in `world-map.js` is unaffected; dark-mode `<img>` filter preserved. **Legend unchanged** — `.geo-swatch.online|offline` already renders 12px theme-colored chips; the screenshot's bare text was a stale deployed build, not a code defect. Render test extended to pin `id="clients"`. **Verified independently:** id present, span selectors correct, map cap + aspect intact, `gofmt`/`go vet` clean, `go test ./...` green (`TestHandleGetPartialClients_RendersGeoMapCard` + `PartialTabs/clients` pass), static linux/amd64 build OK, zero-outbound unchanged. Browser visual confirmation rolls into the manual Slice 5 check.

---

## Items needing attention

| Task/Slice | Issue | Recommendation |
|---|---|---|
| Slice 4 SVG asset | Need a permissively-licensed world-outline SVG | Pick a public-domain / CC0 outline (e.g. a simple Natural Earth-derived SVG); record source+license; keep it embedded (no runtime fetch) |
| Slices 2 & 4 visual | No browser MCP for automated UI/chart/map checks | Render/partial tests + projection unit tests cover logic; the map/timeline visual + offline-network proof is the manual Slice 5 |
| Slice 5 | Needs a real VPN client + deployed binary | Owner runs it; treat as the required manual end-to-end (offline-render proof can't be asserted from a green build) |
